<?xml version="1.0" ?>
<odoo>
    <record id="cron_nullbeleg" model="ir.cron">
        <field name="name">POS: Create a new Nullbeleg on the last day of each month</field>
        <field name="model_id" ref="point_of_sale.model_pos_config"/>
        <field name="state">code</field>
        <field name="code">model._cron_create_monthly_receipt()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">months</field>
        <field name="nextcall" eval="(DateTime.now() + relativedelta(day=1, months=1) - timedelta(days=1)).strftime('%Y-%m-%d 23:00:00')" />
    </record>
</odoo>